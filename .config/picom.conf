# WARNING: Do not edit this file.
# It was generated by processing /home/husjon/.config/picom.conf##default,t.j2

# Shadow {{{# }}}
# Opacity {{{
    inactive-opacity            = 1.0;
    active-opacity              = 1.0;
    frame-opacity               = 0.66; # affects menubars (f.ex pycharm older versions?)
    inactive-opacity-override   = false;
    detect-client-opacity       = true;
    opacity-rule                = [
        "99:window_type = 'popup_menu'",
        "99:window_type = 'dropdown_menu'",
        "20:class_i = 'presel_feedback'",
        "90:class_g = 'Rofi'",
    ];
# }}}
# Dim {{{
    inactive-dim                = 0.1;
    inactive-dim-fixed          = true;
# }}}
# Blur {{{
    blur-background             = true;
    
    resize-damage               = 2;        # Helps fix artifacts with blur (should be blur radius eg: 5x5 = 2)
    blur-background-fixed       = true;     # blur independent of opacity
    blur-background-exclude     = [
        "window_type    = 'dock'",
        "window_type    = 'desktop'",
        "class_g = 'slop'",
    ];
# }}}
# Fading {{{# }}}
# Backend {{{# }}}
# Focus {{{
    focus-exclude               = [
        "class_g = 'slop'",
    ];
# }}}
# Other {{{
    vsync                       = false;
    mark-wmwin-focused          = true;
    mark-ovredir-focused        = false;    # Helps with correcting inactive-dim with bspwm
    detect-rounded-corners      = true;
    refresh-rate                = 0;
    sw-opti                     = false;
    use-ewmh-active-win         = true;


    detect-transient            = true;
    detect-client-leader        = false;    # Corrects dimming no f.ex Firefox

    invert-color-include        = [
    ];
# }}}
# Window type settings {{{
    wintypes:
    {
        tooltip = {         # self-described
            fade    = true;
            shadow  = true;
            opacity = 1;
            focus   = true;
        };
        menu = {            # menubars
            fade    = false;
            shadow  = true;
            opacity = 1;
            focus   = true;
        };
        popup_menu = {      # right click menues
            fade    = false;
            shadow  = true;
            opacity = 1;
            focus   = true;
        };
        dropdown_menu = {   #
            fade    = false;
            shadow  = true;
            opacity = 1;
            focus   = true;
        }
    };
# }}}
